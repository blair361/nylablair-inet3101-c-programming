The first line is a preprocessor directive that contains the definitions the program needs in order to print text to the screen otherwise the compiler wouldn't know what printf is.
The main function is required for every c program and its an int because it will return an integer value when it finished and () means the function doesn't take any inputs and inside {} is everything the program does.
printf is a function defined in stdio.h. \n moves the cursor to the next line after printing.
return 0 tells the program to exit and sends the value 0 to the os and 0 means program finished successfully.
